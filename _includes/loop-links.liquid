{%- for link in site.links -%}
{%- assign this-link = link.url | remove: '/' -%}
{%- if this-link == this-page -%}
{%- assign current-link = true -%}
{%- else -%}
{%- assign current-link = false -%}
{%- endif %}
<a class="nav-link {%- if current-link %} active {%- endif %}" aria-current="page" href="{{ link.url | relative_url }}">{{ link.text }}</a>
{%- endfor -%}